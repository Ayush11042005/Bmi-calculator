spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/bmi_calculator}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:Ayush11042005}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

jwt.secret=${JWT_SECRET:bmiCalculatorSecretKeyForJwtTokenGenerationAndValidation2024SecureKey}

jwt.expiration=86400000

server.port=${PORT:8080}
